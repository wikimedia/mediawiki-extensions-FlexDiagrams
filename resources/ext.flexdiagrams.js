/*
 * ext.flexdiagrams.js
 *
 */

window.flexdiagrams = ( function ( $, undefined ) {
	'use strict';

	/**
	 * Declares methods and properties that are available through the fd
	 * namespace.
	 *
	 * @class fd
	 * @alternateClassName diagrams
	 * @singleton
	 */
	return {

	};
}( jQuery ) );

// Assigning namespace.
window.fd = window.flexdiagrams;// = fd;
